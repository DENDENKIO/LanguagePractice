<UserControl x:Class="LanguagePractice.Views.ExperimentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:LanguagePractice.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Settings & List -->
        <ScrollViewer Grid.Column="0" Margin="0,0,10,0">
            <StackPanel>
                <TextBlock Text="実験 (Experiment)" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

                <GroupBox Header="実験設定" Padding="5" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="タイトル:" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding ExpTitle}" Margin="0,0,0,5" Padding="3"/>

                        <TextBlock Text="共通お題:" Margin="0,0,0,2"/>
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding CommonTopic}" Padding="3"/>
                            <Button Grid.Column="1" Content="選択" Command="{Binding PickCommonTopicCommand}" Margin="5,0,0,0"/>
                        </Grid>

                        <TextBlock Text="書き手 (共通):" Margin="0,0,0,2"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding CommonWriter}" Padding="3"/>
                            <Button Grid.Column="1" Content="選択" Command="{Binding PickCommonWriterCommand}" Margin="5,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="変数: 読者像 (Reader)" Padding="5" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="条件 A:" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding VarValue1}" Margin="0,0,0,5" Padding="3"/>
                        <TextBlock Text="条件 B:" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding VarValue2}" Margin="0,0,0,5" Padding="3"/>
                        <TextBlock Text="条件 C (任意):" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding VarValue3}" Margin="0,0,0,5" Padding="3"/>
                    </StackPanel>
                </GroupBox>

                <Button Content="実験開始" Command="{Binding StartExperimentCommand}" Height="40" Background="#4CAF50" Foreground="White" FontWeight="Bold" Margin="0,0,0,20"/>

                <ItemsControl ItemsSource="{Binding Trials}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,5" Padding="10" CornerRadius="4" BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#F5F5F5"/>
                                        <Setter Property="BorderBrush" Value="#DDD"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                <Setter Property="Background" Value="#E3F2FD"/>
                                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="{Binding VariableValue}" FontWeight="Bold"/>
                                    <TextBlock Text="実行中..." Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}" Foreground="#1565C0"/>
                                    <TextBlock Text="完了" Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVis}}" Foreground="#4CAF50"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Content="次の条件へ >>" Command="{Binding NextTrialCommand}" Height="40" Margin="0,10,0,0" Background="#FF9800" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Right: Run Panel -->
        <Border Grid.Column="1" BorderBrush="#DDD" BorderThickness="1" Padding="10" Background="White">
            <views:RunPanelView DataContext="{Binding RunPanelVM}"/>
        </Border>
    </Grid>
</UserControl>
