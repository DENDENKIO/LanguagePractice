<UserControl x:Class="LanguagePractice.Views.RouteSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Routes (All)" FontSize="24" FontWeight="Bold" Margin="0,0,20,0" VerticalAlignment="Center"/>
            <Button Content="＋新規ルート作成" Command="{Binding CreateNewRouteCommand}" 
                    Background="#4CAF50" Foreground="White" FontWeight="Bold" Padding="15,5" Height="35"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Routes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10" Padding="15" Background="White">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="#1565C0"/>
                                    <TextBlock Text="{Binding Description}" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="開始" 
                                            Command="{Binding DataContext.StartRouteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="15,5" Height="35" Background="#2196F3" Foreground="White" FontWeight="Bold" Margin="0,0,5,0"/>

                                    <Button Content="編集" 
                                            Command="{Binding DataContext.EditRouteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="10,5" Height="35" Margin="0,0,5,0"/>

                                    <Button Content="削除" 
                                            Command="{Binding DataContext.DeleteRouteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="10,5" Height="35" Background="#FFEBEE" Foreground="#D32F2F"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
