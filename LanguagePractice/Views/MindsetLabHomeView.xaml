<UserControl x:Class="LanguagePractice.Views.MindsetLabHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Border Grid.Row="0" Background="#1565C0" Padding="20">
            <Grid>
                <StackPanel>
                    <TextBlock Text="🧠 MindsetLab" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="凡人ではないレベル思考習慣×生活設計トレーニング" 
                               FontSize="12" Foreground="#BBDEFB" Margin="0,5,0,0"/>
                </StackPanel>
                <TextBlock Text="{Binding ConsecutiveDays, StringFormat='継続: {0}日'}" 
                           FontSize="16" Foreground="White" FontWeight="Bold"
                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- 今日のセッション -->
        <Border Grid.Row="1" Background="#E3F2FD" Padding="20">
            <Grid>
                <StackPanel>
                    <TextBlock Text="今日のセッション" FontSize="16" FontWeight="Bold" Foreground="#1565C0"/>
                    <TextBlock Text="{Binding TodayStatus}" FontSize="14" Foreground="#424242" Margin="0,5,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <!-- 今日のセッションがない場合 -->
                    <Button Content="▶ 開始する" 
                            Command="{Binding StartTodayCommand}"
                            Visibility="{Binding HasTodaySession, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                            Padding="20,10" FontSize="14"
                            Background="#4CAF50" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
                    <!-- 今日のセッションがある場合 -->
                    <Button Content="▶ 続ける" 
                            Command="{Binding ContinueTodayCommand}"
                            Visibility="{Binding HasTodaySession, Converter={StaticResource BoolToVisibilityConverter}}"
                            Padding="20,10" FontSize="14"
                            Background="#FF9800" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 履歴 -->
        <Grid Grid.Row="2" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="📋 履歴" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="🔄" Command="{Binding RefreshCommand}" 
                        Padding="8,4" Margin="10,0,0,0" ToolTip="更新"/>
            </StackPanel>

            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding RecentDays}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="1"
                      BorderBrush="#E0E0E0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="日付" Binding="{Binding DateDisplay}" Width="150"/>
                    <DataGridTextColumn Header="重点マインドセット" Binding="{Binding FocusMindsets}" Width="200"/>
                    <DataGridTextColumn Header="スコア" Binding="{Binding ScoreDisplay}" Width="80"/>
                    <DataGridTemplateColumn Header="操作" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="開く" 
                                        Command="{Binding DataContext.OpenDayCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Padding="10,4"
                                        Background="#1565C0" Foreground="White" BorderThickness="0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
