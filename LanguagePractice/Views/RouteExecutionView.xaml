<UserControl x:Class="LanguagePractice.Views.RouteExecutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:LanguagePractice.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Step List -->
        <Border Grid.Column="0" BorderBrush="#DDD" BorderThickness="0,0,1,0" Padding="0,0,10,0">
            <StackPanel>
                <TextBlock Text="{Binding RouteTitle}" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" TextWrapping="Wrap"/>

                <ItemsControl ItemsSource="{Binding Steps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,5" Padding="10" CornerRadius="4" BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#F5F5F5"/>
                                        <Setter Property="BorderBrush" Value="#DDD"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                <Setter Property="Background" Value="#E3F2FD"/>
                                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding OperationName}" FontSize="11" Foreground="Gray"/>
                                    <TextBlock Text="実行中..." Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}" Foreground="#1565C0" FontWeight="Bold"/>
                                    <TextBlock Text="完了" Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVis}}" Foreground="#4CAF50" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Content="次のステップへ進む >>" 
                        Command="{Binding NextStepCommand}"
                        Height="40" Margin="0,20,0,0"
                        Background="#FF9800" Foreground="White" FontWeight="Bold"/>
                <TextBlock Text="※「解析して保存」完了後に押してください" FontSize="11" Foreground="Gray" Margin="0,5,0,0" TextAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Right: Run Panel -->
        <Border Grid.Column="1" Padding="10,0,0,0">
            <views:RunPanelView DataContext="{Binding RunPanelVM}"/>
        </Border>
    </Grid>
</UserControl>
