<UserControl x:Class="LanguagePractice.Views.PracticeSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">

        <!-- Selection Mode -->
        <Grid Visibility="{Binding IsSelectionMode, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="600">
                <TextBlock Text="練習コース選択" FontSize="30" FontWeight="Bold" Margin="0,0,0,30" TextAlignment="Center"/>

                <TextBlock Text="以下のパックから選択してください:" Margin="0,0,0,10"/>

                <ListBox ItemsSource="{Binding AvailablePacks}" SelectedItem="{Binding SelectedPack}" Height="200" BorderBrush="#DDD" BorderThickness="1">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding Description}" Foreground="Gray"/>
                                <TextBlock Text="{Binding TotalMinutes, StringFormat='所要時間: {0}分'}" Foreground="#E65100"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button Content="練習を開始する" Command="{Binding StartSessionCommand}" 
                        Height="50" Margin="0,30,0,0" Background="#4CAF50" Foreground="White" FontWeight="Bold" FontSize="18"/>
            </StackPanel>
        </Grid>

        <!-- Running Mode -->
        <Grid Visibility="{Binding IsRunningMode, Converter={StaticResource BoolToVis}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header & Timer -->
                <RowDefinition Height="*"/>
                <!-- Content -->
                <RowDefinition Height="Auto"/>
                <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{Binding StepTitle}" FontSize="24" FontWeight="Bold" Foreground="#1565C0"/>
                        <TextBlock Text="{Binding StepDescription}" Margin="0,5,0,0" Foreground="Gray"/>
                    </StackPanel>

                    <Border Grid.Column="1" Background="#FFF3E0" CornerRadius="5" Padding="15,5">
                        <TextBlock Text="{Binding TimerText}" FontSize="24" FontWeight="Bold" Foreground="#E65100"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1">
                <Grid Visibility="{Binding IsDrillA, Converter={StaticResource BoolToVis}}">
                    <GroupBox Header="{Binding DrillA_Header}">
                        <TextBox Text="{Binding Session.DrillA_Memo, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Padding="10" FontSize="14"/>
                    </GroupBox>
                </Grid>
                <Grid Visibility="{Binding IsDrillB, Converter={StaticResource BoolToVis}}">
                    <GroupBox Header="{Binding DrillB_Header}">
                        <TextBox Text="{Binding Session.DrillB_Metaphors, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Padding="10" FontSize="14"/>
                    </GroupBox>
                </Grid>
                <Grid Visibility="{Binding IsDrillC, Converter={StaticResource BoolToVis}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <GroupBox Grid.Column="0" Header="{Binding DrillC_DraftHeader}" Margin="0,0,5,0">
                        <TextBox Text="{Binding Session.DrillC_Draft, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Padding="10" FontSize="14"/>
                    </GroupBox>
                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <GroupBox Grid.Row="0" Header="核 (Core)" Height="100" Margin="5,0,0,5">
                            <TextBox Text="{Binding Session.DrillC_Core, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Padding="5"/>
                        </GroupBox>
                        <GroupBox Grid.Row="1" Header="推敲ログ" Margin="5,5,0,0">
                            <TextBox Text="{Binding Session.DrillC_Revision, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Padding="5"/>
                        </GroupBox>
                    </Grid>
                </Grid>
                <Grid Visibility="{Binding IsWrap, Converter={StaticResource BoolToVis}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <GroupBox Grid.Row="0" Header="ベスト1文" Margin="0,0,0,5">
                        <TextBox Text="{Binding Session.Wrap_BestOne, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Padding="10" FontSize="16" FontWeight="Bold"/>
                    </GroupBox>
                    <GroupBox Grid.Row="1" Header="明日の課題" Margin="0,5,0,0">
                        <TextBox Text="{Binding Session.Wrap_Todo, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Padding="10"/>
                    </GroupBox>
                </Grid>
            </Grid>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="一時保存" Command="{Binding SaveDraftCommand}" Width="100" Margin="0,0,20,0"/>
                <Button Content="次のドリルへ >>" Command="{Binding NextStepCommand}" Visibility="{Binding IsNotWrap, Converter={StaticResource BoolToVis}}" Width="150" Height="40" Background="#2196F3" Foreground="White" FontWeight="Bold"/>
                <Button Content="終了して保存" Command="{Binding FinishCommand}" Visibility="{Binding IsWrap, Converter={StaticResource BoolToVis}}" Width="200" Height="40" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
