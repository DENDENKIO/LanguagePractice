<UserControl x:Class="LanguagePractice.Views.PoetryLabProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="← 戻る" Command="{Binding BackCommand}"
                        Padding="15,8" Background="Transparent" Foreground="White" 
                        BorderThickness="1" BorderBrush="#666666" Cursor="Hand"/>

                <StackPanel Grid.Column="1" Margin="20,0,0,0">
                    <TextBlock Text="{Binding Project.Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Foreground="#AAAAAA" FontSize="12" Margin="0,4,0,0">
                        <Run Text="Style:"/>
                        <Run Text="{Binding Project.StyleType}"/>
                        <Run Text=" | Created:"/>
                        <Run Text="{Binding Project.CreatedAt}"/>
                    </TextBlock>
                </StackPanel>

                <Button Grid.Column="2" Content="▶ 標準Run開始" Command="{Binding StartRunCommand}"
                        Padding="20,10" FontSize="14"
                        Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- メインコンテンツ -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左：Run履歴 -->
            <Border Grid.Column="0" BorderBrush="#DDDDDD" BorderThickness="1" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#F5F5F5" Padding="15,10">
                        <TextBlock Text="Run履歴" FontWeight="Bold"/>
                    </Border>

                    <ListBox Grid.Row="1" ItemsSource="{Binding Runs}" 
                             SelectedItem="{Binding SelectedRun}"
                             BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10,8" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock FontWeight="SemiBold">
                                                <Run Text="Run #"/>
                                                <Run Text="{Binding Id}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding CreatedAt}" FontSize="11" Foreground="#888888"/>
                                        </StackPanel>

                                        <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="SUCCESS">
                                                            <Setter Property="Text" Value="✓"/>
                                                            <Setter Property="Foreground" Value="#4CAF50"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="FAILED">
                                                            <Setter Property="Text" Value="✗"/>
                                                            <Setter Property="Foreground" Value="#D32F2F"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="RUNNING">
                                                            <Setter Property="Text" Value="●"/>
                                                            <Setter Property="Foreground" Value="#FF9800"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="CANCELLED">
                                                            <Setter Property="Text" Value="−"/>
                                                            <Setter Property="Foreground" Value="#888888"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- 右：成果物プレビュー -->
            <Border Grid.Column="1" BorderBrush="#DDDDDD" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#F5F5F5" Padding="15,10">
                        <TextBlock Text="成果物プレビュー" FontWeight="Bold"/>
                    </Border>

                    <!-- 成果物タブ -->
                    <Border Grid.Row="1" Padding="10,5" Background="#FAFAFA">
                        <ItemsControl ItemsSource="{Binding Assets}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding AssetType}" 
                                            Command="{Binding DataContext.SelectAssetCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="10,5" Margin="2"
                                            FontSize="11"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>

                    <!-- プレビューエリア -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="15">
                        <TextBox Text="{Binding PreviewText, Mode=OneWay}" 
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 BorderThickness="0"
                                 FontFamily="Consolas, MS Gothic"
                                 FontSize="13"
                                 Background="Transparent"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- フッター -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="継続" Command="{Binding ContinueRunCommand}" 
                            Padding="15,8" Margin="0,0,10,0"
                            IsEnabled="{Binding SelectedRun.Status, Converter={StaticResource RunningToBoolConverter}, FallbackValue=False}"/>
                    <Button Content="エクスポート" Command="{Binding ExportCommand}" 
                            Padding="15,8" Margin="0,0,10,0"/>
                    <Button Content="Issue一覧" Command="{Binding ViewIssuesCommand}" 
                            Padding="15,8" Margin="0,0,10,0"/>
                    <Button Content="比較履歴" Command="{Binding ViewCompareHistoryCommand}" 
                            Padding="15,8"/>
                </StackPanel>

                <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" 
                           VerticalAlignment="Center" Foreground="#666666"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
