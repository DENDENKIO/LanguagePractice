<UserControl x:Class="LanguagePractice.Views.PoetryLabHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="PoetryLab" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="グローバル・リビジョン訓練装置" FontSize="12" Foreground="#AAAAAA" Margin="0,4,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" Content="＋ 新規Project" 
                        Command="{Binding OpenCreateDialogCommand}"
                        Padding="20,10" FontSize="14"
                        Background="#0078D4" Foreground="White" BorderThickness="0"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- フィルター -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="20,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="検索:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Width="200" Padding="8,6"/>

                <TextBlock Text="スタイル:" VerticalAlignment="Center" Margin="20,0,8,0"/>
                <ComboBox ItemsSource="{Binding StyleFilters}" 
                          SelectedItem="{Binding SelectedStyleFilter}"
                          Width="100" Padding="8,6"/>

                <Button Content="更新" Command="{Binding RefreshCommand}" 
                        Margin="20,0,0,0" Padding="15,6"/>
            </StackPanel>
        </Border>

        <!-- プロジェクト一覧 -->
        <Border Grid.Row="2" Margin="20">
            <Grid>
                <!-- リスト -->
                <DataGrid ItemsSource="{Binding Projects}" 
                          SelectedItem="{Binding SelectedProject}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="1"
                          BorderBrush="#DDDDDD">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="タイトル" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="スタイル" Binding="{Binding StyleType}" Width="80"/>
                        <DataGridTextColumn Header="作成日" Binding="{Binding CreatedAt}" Width="150"/>
                        <DataGridTextColumn Header="更新日" Binding="{Binding UpdatedAt}" Width="150"/>
                        <DataGridTemplateColumn Header="操作" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="開く" 
                                                Command="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Padding="10,4" Margin="0,0,5,0"
                                                Background="#0078D4" Foreground="White" BorderThickness="0"/>
                                        <Button Content="削除" 
                                                Command="{Binding DataContext.DeleteProjectCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Padding="10,4"
                                                Background="#D32F2F" Foreground="White" BorderThickness="0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 空状態 -->
                <TextBlock Text="プロジェクトがありません。&#x0a;「＋ 新規Project」から作成してください。"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="#888888" FontSize="14" TextAlignment="Center"
                           Visibility="{Binding Projects.Count, Converter={StaticResource ZeroToVisibleConverter}, FallbackValue=Collapsed}"/>
            </Grid>
        </Border>

        <!-- 新規作成ダイアログ（オーバーレイ） -->
        <Grid Grid.RowSpan="3" 
              Visibility="{Binding IsCreateDialogOpen, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}">
            <!-- 背景オーバーレイ -->
            <Border Background="#80000000"/>

            <!-- ダイアログ本体 -->
            <Border Background="White" 
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Padding="30" CornerRadius="8"
                    MinWidth="400">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Opacity="0.3"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="新規プロジェクト作成" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                    <TextBlock Text="タイトル" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewProjectTitle, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="10,8" FontSize="14"/>

                    <TextBlock Text="スタイル" Margin="0,15,0,5"/>
                    <ComboBox ItemsSource="{Binding StyleOptions}" 
                              SelectedItem="{Binding NewProjectStyle}"
                              Padding="10,8" FontSize="14"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
                        <Button Content="キャンセル" Command="{Binding CancelCreateCommand}" 
                                Padding="20,10" Margin="0,0,10,0"/>
                        <Button Content="作成" Command="{Binding CreateProjectCommand}"
                                Padding="20,10"
                                Background="#0078D4" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
